// Shared connection state for cross-component communication
// Used when we need to share state between components outside React context

export const connectionState = {
  isConnectionClosed: false,
};
